seasonPacks and showPacks are only available if the addon calling OpenScrapers supports this.  Without addon support the enabled pack scrapers will function as before and provide sources for single episodes and movies only!